--- src/file_.cpp.orig	Fri Oct 23 07:54:51 2009
+++ src/file_.cpp	Fri Oct 23 13:06:01 2009
@@ -407,11 +407,7 @@
     if (gzipIO==ARCH_GZIP)  {
 #ifndef _MVS
       p = NULL;
-#ifdef __WIN32__
-      CreateConcat  ( p,gzip_path,pstr(" | cat > "),FName );
-#else
       CreateConcat  ( p,gzip_path,pstr(" > "),FName );
-#endif
       hFile = popen ( p,"w" );
       if (p)  delete[] p;
 #else
